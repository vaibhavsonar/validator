# Tell Lombok this is the root directory; do not scan parent folders
config.stopBubbling = true

# Add @lombok.Generated to all generated methods so JaCoCo skips them
lombok.addLombokGeneratedAnnotation = true
